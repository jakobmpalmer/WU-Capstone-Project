<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.willamette_thesis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/eco_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="setting_activity"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".ProfileActivity"
            android:label="profile_activity"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".WasteActivity"
            android:label="waste_activity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".LogFragment">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".LogActivity"> -->
        <!-- <intent-filter> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".CarActivity" />
        <activity
            android:name=".ConsumableActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity> <!-- <activity android:name=".HomeActivity" /> -->
        <activity
            android:name=".LogNavigator"
            android:label="log_navigator"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>